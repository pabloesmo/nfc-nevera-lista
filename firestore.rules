rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // shoppingList: any authenticated user can read/write
    // For this family app we keep it open but only for the known user IDs
    match /shoppingList/{item} {
      allow read, write: if true; // tighten once Firebase Auth is added
    }
  }
}
